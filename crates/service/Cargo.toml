[package]
name = "roadwork-service"
version = "0.1.0"
edition = "2024"
description = "Shared access to opendata service descriptors"

[dependencies]
roadwork-core = { path = "../core", version = "0.1.0" }
roadwork-sync = { path = "../sync", version = "0.1.0" }
base64 = "0.22"
log = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
